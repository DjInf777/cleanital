import assert from 'node:assert/strict';import { mkdtemp, readFile } from 'node:fs/promises';import { tmpdir } from 'node:os';import { join } from 'node:path';import { processSubmission, validateSubmission } from '../server/forms.mjs';
const base={contactName:'Test Person',organizationName:'Test Business',email:'person@example.com',phone:'555-555-0100',state:'North Carolina',facilityType:'Office or professional workplace',serviceNeeded:'Office cleaning',message:'We need a commercial office cleaning assessment for an occupied workplace.',consent:'yes',startedAt:String(Date.now()-5000),website:''};
assert.equal(validateSubmission('assessment',base).ok,true);assert.equal(validateSubmission('assessment',{...base,email:'bad'}).ok,false);assert.equal(validateSubmission('assessment',{...base,website:'spam'}).ok,false);assert.equal(validateSubmission('assessment',{...base,startedAt:String(Date.now())}).ok,false);
const dir=await mkdtemp(join(tmpdir(),'cleanital-'));const result=await processSubmission({kind:'assessment',input:base,ip:'127.0.0.1',userAgent:'test',storageDir:dir,ipHashKey:'test-key-long-enough'});assert.equal(result.ok,true);const day=new Date().toISOString().slice(0,10);const stored=await readFile(join(dir,`${day}.jsonl`),'utf8');assert.match(stored,/CLN-/);assert.doesNotMatch(stored,/127\.0\.0\.1/);console.log('Form validation and durable-write tests passed.');
